#pragma once

namespace Spacetime {

enum class MetricKind {
    Schwarzschild,
};

enum class CoordinateChartKind {
    SchwarzschildSpherical, // X = (t, r, theta, phi)
};

} // namespace Spacetime
